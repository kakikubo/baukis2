# This file contains a list of dependencies that are not part of the long term plan for the
# '.' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
app/packages/customer:
  "::Customer":
    violations:
    - dependency
    files:
    - app/forms/staff/customer_form.rb
    - db/seeds/development/customers.rb
    - spec/system/staff/customer_management_spec.rb
app/packages/rails_shims:
  "::Address::PREFECTURE_NAMES":
    violations:
    - dependency
    files:
    - app/views/staff/customers/_home_address_fields.html.erb
    - app/views/staff/customers/_work_address_fields.html.erb
    - db/seeds/development/customers.rb
  "::Admin::Base::TIMEOUT":
    violations:
    - dependency
    files:
    - spec/requests/admin/staff_members_management_spec.rb
  "::Administrator":
    violations:
    - dependency
    files:
    - db/seeds/development/administrators.rb
    - spec/models/administrator_spec.rb
  "::ApplicationController::IpAddressRejected":
    violations:
    - dependency
    files:
    - app/views/errors/forbidden.html.erb
  "::Staff::Base::TIMEOUT":
    violations:
    - dependency
    files:
    - spec/requests/staff/my_accounts_management_spec.rb
  "::StaffMember":
    violations:
    - dependency
    files:
    - db/seeds/development/staff_events.rb
    - db/seeds/development/staff_members.rb
    - spec/models/staff_member_spec.rb
